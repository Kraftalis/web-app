[phases.setup]
nixPkgs = ["nodejs_24"]

[phases.install]
cmds = ["yarn install", "npx prisma generate"]

[phases.build]
cmds = ["yarn build"]

[start]
cmd = "yarn start"

[variables]
NODE_ENV = "production"
PORT = "3000"
