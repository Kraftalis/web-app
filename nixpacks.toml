[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = [
  "yarn install",
  "npx prisma generate",
  "yarn build"
]

[start]
cmd = "yarn start"

[variables]
NODE_ENV = "production"
PORT = "3000"
